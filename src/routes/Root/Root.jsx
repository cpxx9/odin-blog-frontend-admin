import Posts from '../../components/BlogPosts/Posts/Posts';
import StyledRoot from './StyledRoot';

const Root = () => {
  return (
    <StyledRoot>
      <h2>Posts</h2>
      <Posts />
    </StyledRoot>
  );
};

export default Root;
